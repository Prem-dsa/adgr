import CategoryPage from '../../../components/CategoryPage';
import { drySkinCategory } from '../../../data/categoryData';

const DrySkinIndex = () => {
    return <CategoryPage category={drySkinCategory} />;
};

export default DrySkinIndex;
