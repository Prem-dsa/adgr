import CategoryPage from '../../../components/CategoryPage';
import { retentionCategory } from '../../../data/categoryData';

const RetentionIndex = () => {
    return <CategoryPage category={retentionCategory} />;
};

export default RetentionIndex;
