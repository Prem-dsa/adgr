import CategoryPage from '../../../components/CategoryPage';
import { replacementCategory } from '../../../data/categoryData';

const ReplacementIndex = () => {
    return <CategoryPage category={replacementCategory} />;
};

export default ReplacementIndex;
